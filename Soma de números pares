#include <stdio.h>

int main()
{
    int num, soma = 0;

    for(num = 1; num <= 100; num++)
    {
        if(num % 2 == 0)
        {
            soma = soma + num;
        }
    }

    printf("Esse eh resultado da soma dos numeros pares: %d\n", soma);

    return 0;
}
